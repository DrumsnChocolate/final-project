data:
  name: 'zgt-binary'
  root: 'data/zgt/zgt-linked'
  image_extension: '.png'
  annotation_extension: '.png'
  preprocess:
    - name: resize
      dimensions: [ 512, 512 ]
      mode: bilinear
  test:
    image_dir: images
    annotation_dir: annotations_binary
    batch_size: 1
