Meant for running some unit tests, to make sure that things work as expected.

(make sure you execute below commands from the root of the segment_anything folder)
```bash
conda activate segment_anything
cd finetune/test
pytest
```